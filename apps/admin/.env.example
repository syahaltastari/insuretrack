# Admin env (contoh). Salin ke .env.local saat dev lokal.

# Base URL backend API. Di Docker compose, "backend" adalah hostname service.
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Port dev server (default 3001).
ADMIN_PORT=3001
